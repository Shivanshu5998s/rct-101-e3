import React from "react";
import { Products } from "../components/Products";

const Home = () => {
  return (
    <div
      style={{
        maxWidth: "100%px",
        margin: "auto",
      }}
    > {/* Code here */}
      <Products />
    </div>
  );
};

export default Home;
